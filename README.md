markdown
# ASMO Backend Service

Backend сервис для управления проектами веб-приложений, мобильных приложений и ботов.

## 🏗️ Структура проекта
ASMO_site_backend/
├── backend/ # Go backend сервер
├── nginx/ # Nginx reverse proxy
├── docker-compose.yml # Основной Docker Compose
├── docker-compose.test.yml # Тестовый Docker Compose
└── README.md

text

## 🚀 Быстрый старт

### Предварительные требования
- Docker Desktop
- Go 1.25.4+

### Запуск проекта

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd ASMO_site_backend
Запустите сервисы:

bash
run.bat
Или вручную:

bash
docker-compose up --build
Проверьте работу:

bash
curl http://localhost/api/health
 API Endpoints
Health Check
GET /api/health - Проверка состояния сервиса

Web Applications
GET /api/WebApplications/:id - Получить веб-проект по ID

POST /api/WebApplications - Создать веб-проект

Mobile Applications
GET /api/MobileApplications/:id - Получить мобильный проект по ID

POST /api/MobileApplications - Создать мобильный проект

Bots
GET /api/Bots/:id - Получить бот-проект по ID

POST /api/Bots - Создать бот-проект

🧪 Тестирование
Запуск всех тестов:
bash
make.bat test
Только unit тесты:
bash
make.bat test-unit
Только интеграционные тесты:
bash
make.bat test-integration
🗃️ Миграции базы данных
Применить миграции:
bash
make.bat migrate-up
Откатить миграции:
bash
make.bat migrate-down
🐳 Docker команды
Запуск сервисов:
bash
docker-compose up --build
Остановка сервисов:
bash
docker-compose down
Просмотр логов:
bash
docker-compose logs -f backend
🔧 Разработка
Локальный запуск без Docker:
bash
cd backend
go run cmd/server/main.go
Сборка приложения:
bash
make.bat build
📊 Модели данных
WebProjects
id - Уникальный идентификатор

name - Название (15-100 символов)

description - Описание (20-1500 символов)

img - URL изображения

price - Цена (≥ 0)

time_develop - Время разработки (1-1825 дней)

MobileProjects
Аналогичная структура WebProjects

BotsProjects
Аналогичная структура WebProjects

🔐 Валидация
Все входные данные проходят валидацию:

Обязательные поля

Длина строк

Валидные URL

Положительные цены

Временные ограничения

📝 Логирование
Структурированное логирование в JSON формате с уровнями:

DEBUG

INFO

WARN

ERROR

🛠️ Утилиты
run.bat - Запуск всех сервисов

stop.bat - Остановка сервисов

make.bat - Утилита для разработки

📞 Поддержка
При возникновении проблем проверьте:

Запущен ли Docker

Открыты ли порты 80, 8080, 5432

Корректность файла .env