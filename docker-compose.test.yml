version: '3.8'

services:
  test-database:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=testdb
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
    ports:
      - "5433:5432"
    networks:
      - test-network

networks:
  test-network:
    driver: bridge